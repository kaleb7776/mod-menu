local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local player = Players.LocalPlayer

-- Configurações de Voo
local VELOCIDADE = 150

local function getRoot()
    local character = player.Character or player.CharacterAdded:Wait()
    return character:WaitForChild("HumanoidRootPart")
end

-- FUNÇÃO DE PRESSIONAR MELHORADA
local function usarPrompt(prompt)
    if not prompt or not prompt.Enabled then return end
    
    -- Pequena espera para o jogo registrar que você chegou
    task.wait(0.3)
    
    if fireproximityprompt then 
        fireproximityprompt(prompt) 
    else
        prompt:InputHoldBegin()
        task.wait(prompt.HoldDuration + 0.2)
        prompt:InputHoldEnd()
    end
end

local function equiparItem1()
    local bp = player:WaitForChild("Backpack")
    local item = bp:GetChildren()[1]
    if item then 
        player.Character.Humanoid:EquipTool(item) 
    end
end

-- Voo estilo IFOOD (Ajustado para parar mais perto)
local function voarAte(pos)
    local root = getRoot()
    -- Parando em 2 de distância para garantir que o prompt apareça
    while (Vector3.new(root.Position.X, 0, root.Position.Z) - Vector3.new(pos.X, 0, pos.Z)).Magnitude > 2 do
        local dt = RunService.Heartbeat:Wait()
        local dir = (Vector3.new(pos.X, 0, pos.Z) - Vector3.new(root.Position.X, 0, root.Position.Z)).Unit
        local novaPos = root.Position + (dir * VELOCIDADE * dt)
        
        local result = workspace:Raycast(novaPos + Vector3.new(0, 50, 0), Vector3.new(0, -100, 0))
        local y = result and (result.Position.Y + 3) or root.Position.Y
        root.CFrame = CFrame.new(novaPos.X, y, novaPos.Z)
    end
end

local function realizarProcessoEscada()
    local escadaProxy = workspace.CarrosSpawnados.Eletricista.Body.Escada.Proxy
    voarAte(escadaProxy.Position)
    usarPrompt(escadaProxy.ProximityPrompt)
    task.wait(0.2) 
    equiparItem1()
end

-- ==========================================
-- LOOP INFINITO
-- ==========================================

task.wait(5)

while true do
    -- --- POSTE 30 ---
    realizarProcessoEscada()
    task.wait(2)
    local poste30 = workspace.Complementos.Postes:GetChildren()[30]
    voarAte(poste30.proxymiti.Position)
    usarPrompt(poste30.proxymiti.ProximityPrompt)

    task.wait(3)
    voarAte(poste30.Poste.Position)
    usarPrompt(poste30.Poste.ProximityPrompt)

    task.wait(2)
    voarAte(poste30.proxymiti.Position)

    -- --- POSTE 28 ---
    task.wait(1)
    realizarProcessoEscada()
    task.wait(2)
    local poste28 = workspace.Complementos.Postes:GetChildren()[28]
    voarAte(poste28.proxymiti.Position)
    usarPrompt(poste28.proxymiti.ProximityPrompt)

    task.wait(2)
    voarAte(poste28.Poste.Position)
    task.wait(3)
    usarPrompt(poste28.Poste.ProximityPrompt)

    task.wait(1)
    voarAte(poste28.proxymiti.Position)

    -- --- POSTE 29 ---
    task.wait(2)
    realizarProcessoEscada()
    task.wait(2)
    local poste29 = workspace.Complementos.Postes:GetChildren()[29]
    voarAte(poste29.proxymiti.Position)
    usarPrompt(poste29.proxymiti.ProximityPrompt)

    task.wait(2)
    voarAte(poste29.Poste.Position)
    usarPrompt(poste29.Poste.ProximityPrompt)

    task.wait(2)
    voarAte(poste29.proxymiti.Position)

    print("Ciclo concluído. Reiniciando em 8s...")
    task.wait(22) 
end
